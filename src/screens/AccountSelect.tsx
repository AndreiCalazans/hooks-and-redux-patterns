import * as React from "react";
import { texts } from "../utilities/texts";

export const AccountSelect: React.FC = () => {
  return (
    <div>
      <h1>{texts.select_account}</h1>
    </div>
  );
};
